namespace BackEnd_Destino.Models;

public enum OfferStatus
{
    Active,
    Inactive,
    Expired,
    SoldOut
}

public enum PurchaseStatus
{
    Pending,
    Accepted,
    Canceled,
    Refunded
}
